//import { Router } from 'express';
//import { UserController } from '../controllers/user.controller'; // Ensure this file exists at the specified path
//import { jwtAuthMiddleware } from '../middlewares/jwtAuth.middleware';
//
//export const userRouter = Router();
//
//userRouter.post('/', jwtAuthMiddleware, UserController.createUser);
//userRouter.get('/:userId', jwtAuthMiddleware, UserController.getUser);
